# contracts.yml
#
# Source of truth for the contracts the ETL must execute (SJRA-1546, section 3.1).
# One entry per source under `sources.<name>`:
#   table_prefix  - Prefix to which the `v{MAJOR}` suffix is appended to name the Iceberg table.
#   contracts     - One entry per MAJOR:
#       lineage       - "{MAJOR}.{MINOR}" the contract corresponds to (dot separated).
#       release_notes - Path to the release notes for that lineage's MAJOR, relative to `spark/`.
#
# A new MINOR bumps `lineage` on the existing row. A new MAJOR adds a new row with its own `lineage` and `release_notes`.
sources:
  "1000_genomes":
    table_prefix: "1000_genomes"
    contracts:
      - lineage: "1.0"
        release_notes: "doc/release-notes/1000_genomes/v1.md"
  clinvar:
    table_prefix: "clinvar"
    contracts:
      - lineage: "1.0"
        release_notes: "doc/release-notes/clinvar/v1.md"
  dbsnp:
    table_prefix: "dbsnp"
    contracts:
      - lineage: "1.0"
        release_notes: "doc/release-notes/dbsnp/v1.md"
  gnomad_cnv:
    table_prefix: "gnomad_cnv"
    contracts:
      - lineage: "1.0"
        release_notes: "doc/release-notes/gnomad_cnv/v1.md"
  gnomad_joint:
    table_prefix: "gnomad_joint"
    contracts:
      - lineage: "1.0"
        release_notes: "doc/release-notes/gnomad_joint/v1.md"
  gnomad_sv:
    table_prefix: "gnomad_sv"
    contracts:
      - lineage: "1.0"
        release_notes: "doc/release-notes/gnomad_sv/v1.md"
  mondo:
    table_prefix: "mondo"
    contracts:
      - lineage: "1.0"
        release_notes: "doc/release-notes/mondo/v1.md"
