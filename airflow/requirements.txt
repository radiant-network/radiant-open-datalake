apache-airflow==3.0.6
apache-airflow-providers-amazon==9.12.0

# Necessary for the @task.virtualenv decorator
cloudpickle==3.1.1

# Explicitly adding 'virtualenv' to use the @task.virtualenv decorator.
# There is a warning indicating that the extra is not provided when using apache-airflow[virtualenv]
# as instructed in the airflow documentation.
virtualenv==20.34.0
