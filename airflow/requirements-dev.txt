pytest==8.4.1
requests==2.32.5 # used in a virtualenv task
requests-mock==1.12.1
ruff==0.12.1
stream-unzip==0.0.101 # test-only: exercises the container stream-unzip transfer (prod dep lives in requirements-operator.txt)